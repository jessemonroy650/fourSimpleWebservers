/*
 * Date: 2014-04-02
 * Returns: Large string that includes the 'http' type
 *
 */

var ldfile     = require('./loadfile');

exports.getMimeType = function(filename, mimetye) {
    theFile = ldfile.asText(fqfilename);
    return theFile;
}
